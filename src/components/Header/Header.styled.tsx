import styled from "styled-components";

export const HeaderWrapper = styled.div`
	display: flex;
	flex-direction: column;
	margin-bottom: 5%;
	flex: 0 1 auto;
`

export const HeaderContainer = styled.div`
	background: #940000;
	flex: 0 0 auto;
`

export const HeaderLogos = styled.div`
	position: relative;
	display: flex;
	margin-top: 1rem;
	margin: 0.8rem 10% 0;
	justify-content: center;
`

export const InvisibleTextLogos = styled.p`
	margin: 0;
	visibility: hidden;
`
